classTweaker v1 official

# Read the slot the mouse is hovering in any container screen, so we can show the NBT of the item
# under the cursor (feature: "items in inventory"). 26.x uses ClassTweaker instead of AccessWidener.
accessible field net/minecraft/client/gui/screens/inventory/AbstractContainerScreen hoveredSlot Lnet/minecraft/world/inventory/Slot;
